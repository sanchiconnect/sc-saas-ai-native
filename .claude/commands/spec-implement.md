---
description: Implement an approved feature spec across the affected repos.
argument-hint: <spec-id>
---
Delegate to the spec-implementer subagent for spec id: $ARGUMENTS.
If status is still `draft`, summarize what needs sign-off and stop instead of implementing.
