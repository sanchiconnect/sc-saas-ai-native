# operate/control-plane/cost-config.yaml
# Generated by ControlPlane — SpecPod Operate Phase v2.1.0
# READ BY: RuntimeIQ (scaling bounds), ControlPlane monitor
# DO NOT EDIT MANUALLY — regenerate via ControlPlane skill

version: "1.0.0"
generated_at: "2025-01-01T00:00:00Z"
currency: USD

agents:
  summarisation-api:
    monthly_ceiling: 500.00
    warning_threshold_pct: 70         # Alert at 70% = $350
    scaling:
      min_replicas: 2
      max_replicas: 8
    cost_per_1k_tokens:
      input: 0.003
      output: 0.015
    model: "claude-haiku-4-5-20251001"

  classification-api:
    monthly_ceiling: 200.00
    warning_threshold_pct: 70         # Alert at 70% = $140
    scaling:
      min_replicas: 1
      max_replicas: 5
    cost_per_1k_tokens:
      input: 0.003
      output: 0.015
    model: "claude-haiku-4-5-20251001"

  recommendation-api:
    monthly_ceiling: 800.00
    warning_threshold_pct: 70         # Alert at 70% = $560
    scaling:
      min_replicas: 2
      max_replicas: 10
    cost_per_1k_tokens:
      input: 0.003
      output: 0.015
    model: "claude-haiku-4-5-20251001"

security:
  anomaly_sensitivity: medium
  baseline_window_days: 7             # Days to observe before alerting
  alert_on:
    call_volume_multiplier: 2.0       # Alert if >2x normal volume
    off_hours_spike: true
    new_ip_principals: true

alerting:
  channel: slack
  webhook_env_var: ALERT_WEBHOOK_URL
