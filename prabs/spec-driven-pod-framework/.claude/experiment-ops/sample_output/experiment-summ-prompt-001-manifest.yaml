# operate/experiment-ops/experiment-summ-prompt-001-manifest.yaml
# Generated by ExperimentOps — SpecPod Operate Phase v2.1.0
# POD Lead sign-off required before activation

experiment_id: summ-prompt-001
status: pending_activation           # Set to 'active' by POD Lead after sign-off
generated_at: "2025-01-15T10:00:00Z"

hypothesis: >
  We believe that switching to a structured bullet-point output format will cause
  average_handle_time_seconds to decrease by 15% for support agents
  because structured summaries reduce re-reading time and key-fact extraction time.

variants:
  control:
    description: "Current summarisation prompt (prose output)"
    endpoint_config: "X-Experiment-Variant: control"
    traffic_allocation_pct: 50

  treatment_a:
    description: "Structured bullet-point prompt (≤5 bullets + key facts)"
    endpoint_config: "X-Experiment-Variant: treatment_a"
    traffic_allocation_pct: 50

primary_metric:
  name: average_handle_time_seconds
  direction: decrease
  minimum_detectable_effect: 0.10    # 10% decrease
  measurement_source: "operate/value-tracker/value-tracker-fetcher.py"

guardrails:
  error_rate_5xx_pct:
    max_degradation_pct: 10          # Auto-stop if 5xx rate increases >10%
  latency_p99_ms:
    max_degradation_pct: 20          # Auto-stop if p99 increases >20%
  user_satisfaction_score:
    max_degradation_pct: 5           # Auto-stop if CSAT drops >5%

statistical_parameters:
  significance_level: 0.95
  minimum_runtime_days: 7
  maximum_runtime_days: 30
  evaluation_cadence: daily_after_minimum_runtime

routing:
  method: user_id_hash               # Consistent assignment per user
  implementation: "operate/experiment-ops/traffic-router.py"

auto_promotion:
  enabled: true
  requires_pod_lead_confirmation: true   # HITL gate before 100% traffic shift

hitl_gates:
  - gate: experiment_go_live
    reviewer: "POD Lead + Stakeholder"
    status: pending
    signed_off_at: null
    signed_off_by: null

  - gate: winner_promotion
    reviewer: "POD Lead"
    status: pending
    signed_off_at: null
    signed_off_by: null
